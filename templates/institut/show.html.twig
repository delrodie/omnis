{% extends 'base.html.twig' %}

{% block title %}Institut{% endblock %}

{% block body %}
    <h1>Institut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ institut.id }}</td>
            </tr>
            <tr>
                <th>Sigle</th>
                <td>{{ institut.sigle }}</td>
            </tr>
            <tr>
                <th>Denomination</th>
                <td>{{ institut.denomination }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('institut_index') }}">back to list</a>

    <a href="{{ path('institut_edit', {'id': institut.id}) }}">edit</a>

    {{ include('institut/_delete_form.html.twig') }}
{% endblock %}
