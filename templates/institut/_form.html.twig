{{ form_start(form) }}
    <div class="form-group row">
        <label class="col-lg-3 col-form-label">Sigle</label>
        <div class="col-lg-9">
            {{ form_widget(form.sigle) }}
            {{ form_errors(form.sigle) }}
        </div>
    </div>
    <div class="form-group row">
        <label class="col-lg-3 col-form-label">Dénomination</label>
        <div class="col-lg-9">
            {{ form_widget(form.denomination) }}
            {{ form_errors(form.denomination) }}
        </div>
    </div>
    {{ form_widget(form) }}
    <div class="text-right mt-5">
        <a href="{{ path('backend_institut_index') }}" class="btn btn-outline-secondary">{{ 'btnCancel'|trans }}</a>
        <button class="btn btn-primary">{{ button_label|default('btnSave'|trans) }}</button>
    </div>

{{ form_end(form) }}