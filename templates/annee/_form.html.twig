{{ form_start(form) }}
<div class="form-group row">
    <label class="col-lg-3 col-form-label">Prefixe</label>
    <div class="col-lg-9">
        {{ form_widget(form.prefixe) }}
        {{ form_errors(form.prefixe) }}
    </div>
</div>
<div class="form-group row">
    <label class="col-lg-3 col-form-label">Année</label>
    <div class="col-lg-9">
        {{ form_widget(form.libelle) }}
        {{ form_errors(form.libelle) }}
    </div>
</div>
    {{ form_widget(form) }}
    <div class="text-right mt-5">
        <a href="{{ path('backend_annee_index') }}" class="btn btn-outline-secondary">Annuler</a>
        <button class="btn btn-primary">{{ button_label|default('btnSave'|trans) }}</button>
    </div>
{{ form_end(form) }}
