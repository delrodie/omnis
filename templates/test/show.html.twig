{% extends 'base.html.twig' %}

{% block title %}Test{% endblock %}

{% block body %}
    <h1>Test</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ test.nombre }}</td>
            </tr>
            <tr>
                <th>Lundi</th>
                <td>{{ test.lundi ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Mardi</th>
                <td>{{ test.mardi ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Mercredi</th>
                <td>{{ test.mercredi ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Jeudi</th>
                <td>{{ test.jeudi ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Vendredi</th>
                <td>{{ test.vendredi ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Samedi</th>
                <td>{{ test.samedi ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Dimanche</th>
                <td>{{ test.dimanche ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('test_index') }}">back to list</a>

    <a href="{{ path('test_edit', {'id': test.id}) }}">edit</a>

    {{ include('test/_delete_form.html.twig') }}
{% endblock %}
