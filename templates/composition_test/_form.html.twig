{{ form_start(form) }}
    <div class="form-group row">
        <div class="col-md-2 text-right">{{ form_label(form.resultat) }}</div>
        <div class="col-md-6">
            {{ form_widget(form.resultat) }}
        </div>
        {{ form_widget(form) }}
        <div class="col-md-3">
            <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        </div>
    </div>

{{ form_end(form) }}
