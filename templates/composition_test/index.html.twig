{% extends 'base.html.twig' %}

{% block title %}CompositionTest index{% endblock %}

{% block body %}
    <h1>CompositionTest index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Ordre</th>
                <th>Resultat</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for composition_test in composition_tests %}
            <tr>
                <td>{{ composition_test.id }}</td>
                <td>{{ composition_test.date }}</td>
                <td>{{ composition_test.ordre }}</td>
                <td>{{ composition_test.resultat }}</td>
                <td>
                    <a href="{{ path('composition_test_show', {'id': composition_test.id}) }}">show</a>
                    <a href="{{ path('composition_test_edit', {'id': composition_test.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('composition_test_new') }}">Create new</a>
{% endblock %}
