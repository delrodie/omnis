{{ form_start(form) }}
    <div class="form-group row">
        <label class="col-lg-3 col-form-label">Institut</label>
        <div class="col-lg-9">
            {{ form_widget(form.institut) }}
            {{ form_errors(form.institut) }}
        </div>
    </div>
    <div class="form-group row">
        <label class="col-lg-3 col-form-label">Filière</label>
        <div class="col-lg-9">
            {{ form_widget(form.libelle) }}
            {{ form_errors(form.libelle) }}
        </div>
    </div>
    {{ form_widget(form) }}
    <div class="text-right mt-5">
        <a href="{{ path('backend_filiere_index') }}" class="btn btn-outline-secondary">{{ 'btnCancel'|trans }}</a>
        <button class="btn btn-primary">{{ button_label|default('btnSave'|trans) }}</button>
    </div>
{{ form_end(form) }}
