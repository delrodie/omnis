{{ form_start(form) }}
    <div class="form-group row">
        <label class="col-lg-3 col-form-label">Niveau</label>
        <div class="col-lg-9">
            {{ form_widget(form.classe) }}
            {{ form_errors(form.classe) }}
        </div>
    </div>
    <div class="form-group row">
        <label class="col-lg-3 col-form-label">Filière</label>
        <div class="col-lg-9">
            {{ form_widget(form.filiere) }}
            {{ form_errors(form.filiere) }}
        </div>
    </div>
    <div class="form-group row">
        <label class="col-lg-3 col-form-label">Monant</label>
        <div class="col-lg-9">
            {{ form_widget(form.montant) }}
            {{ form_errors(form.montant) }}
        </div>
    </div>
    {{ form_widget(form) }}
    <div class="text-right mt-5">
        <button class="btn btn-primary">{{ button_label|default('btnSave'|trans) }}</button>
    </div>

{{ form_end(form) }}
